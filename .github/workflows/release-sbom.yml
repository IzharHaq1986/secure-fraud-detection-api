name: Release SBOM

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write
  packages: read

jobs:
  release_sbom:
    name: Attach SBOM to GitHub Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GHCR (for private manifest access)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Install crane
        uses: imjasonh/setup-crane@v0.3

      - name: Create SBOM (SPDX JSON) from container image digest
        shell: bash
        env:
          IMAGE_REPO: ghcr.io/izharhaq1986/secure-fraud-detection-api
        run: |
          set -euo pipefail

          # Resolve tag name safely under strict mode.
          TAG="${TAG:-${GITHUB_REF_NAME:-}}"
          if [ -z "${TAG}" ]
          then
            echo "Tag is empty"
            exit 1
          fi

          IMAGE_REF="${IMAGE_REPO}:${TAG}"
          echo "Resolving digest for ${IMAGE_REF}"

          # Workflows triggered by the same tag push run in parallel.
          # The publish workflow may still be pushing the manifest when this job starts.
          # Retry for a bounded period to avoid failing on transient 404/MANIFEST_UNKNOWN.
          DIGEST=""
          for attempt in $(seq 1 30)
          do
            if DIGEST="$(crane digest "${IMAGE_REF}" 2>/dev/null)"
            then
              break
            fi

            echo "Digest not available yet for ${IMAGE_REF} (attempt ${attempt}/30). Waiting 10s..."
            sleep 10
          done

          if [ -z "${DIGEST}" ]
          then
            echo "Failed to resolve digest for ${IMAGE_REF} after retries"
            exit 1
          fi

          IMAGE_DIGEST_REF="${IMAGE_REPO}@${DIGEST}"
          echo "Generating SBOM for ${IMAGE_DIGEST_REF}"

          # Install Syft and generate an SPDX JSON SBOM.
          curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh \
            | sh -s -- -b /usr/local/bin

          syft "${IMAGE_DIGEST_REF}" -o spdx-json=sbom.spdx.json

      - name: Create GitHub Release if missing
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          # Resolve tag name safely under strict mode.
          TAG="${TAG:-${GITHUB_REF_NAME:-}}"
          if [ -z "${TAG}" ]
          then
            echo "Tag is empty"
            exit 1
          fi

          # Create a standard (mutable) release if it does not exist yet.
          if ! gh release view "${TAG}" >/dev/null 2>&1
          then
            gh release create "${TAG}" \
              --title "${TAG}" \
              --notes "Automated release for ${TAG}"
          fi

      - name: Upload SBOM to GitHub Release
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          # Resolve tag name safely under strict mode.
          TAG="${TAG:-${GITHUB_REF_NAME:-}}"
          if [ -z "${TAG}" ]
          then
            echo "Tag is empty"
            exit 1
          fi

          if [ ! -f "sbom.spdx.json" ]
          then
            echo "SBOM file not found: sbom.spdx.json"
            exit 1
          fi

          # Upload the SBOM asset. Overwrite if the asset already exists.
          gh release upload "${TAG}" "sbom.spdx.json" --clobber
